<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cardie">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".LoginScreen"></activity>
        <activity android:name=".Settings" />
        <activity android:name=".AddCard" />
        <activity android:name=".TestMode1" />
        <activity android:name=".ResultScreen" />
        <activity android:name=".SetViewActivity" />
        <activity android:name=".UserProfile" />
        <activity android:name=".TestModeActivity" />
        <activity android:name=".card_single" />
        <activity android:name=".setlist_main" />
        <activity android:name=".Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".ReminderBroadcast"
            android:enabled="true"
            android:exported="false"/>
    </application>

</manifest>